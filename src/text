{

    var isEven;

    var isOdd = fun (n) {
        if (n == 0) return false;
        return isEven(n - 1);
    };

    isEven = fun (n) {
      if (n == 0) return true;
      return isOdd(n - 1);
    };

    print isOdd(3);
    print isEven(3);

}
